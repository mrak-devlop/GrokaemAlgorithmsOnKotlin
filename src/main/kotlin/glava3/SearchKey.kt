package glava3

class SearchKey() {
    lateinit var item: String
    fun lookForKey(box: String){
      while (item = "key"){
          if (item = "box" ){
              lookForKey(item)
          } else
              item = "key"
      }
    }
}